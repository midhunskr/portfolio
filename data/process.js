/**
 * "How I Think" process content — the repeatable method steps.
 *
 * Phase 1: placeholder structure + typedefs only. Real content in Phase 7.
 *
 * @typedef {Object} ProcessStep
 * @property {string} num          Two-digit ordinal, e.g. "01".
 * @property {'green'|'amber'|'muted'} tone Number color tone.
 * @property {string} heading
 * @property {string} body
 * @property {number} delay        Reveal delay in ms (original: 0/90/180/270/360).
 */

/** @type {ProcessStep[]} */
export const processSteps = [
  // Placeholder — real steps migrated in Phase 7.
];
