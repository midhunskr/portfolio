import styles from './Navbar.module.css';

/**
 * Navbar shell (Phase 1 placeholder).
 * Real implementation — logo, pill links, scroll condensation, mobile drawer —
 * lands in Phase 2.
 */
export function Navbar() {
  return (
    <nav className={styles.placeholder} aria-label="Main navigation">
      Navbar
    </nav>
  );
}
