import styles from './ContactSection.module.css';

/**
 * Contact section shell (Phase 1 placeholder).
 * Real journey path, magnetic email CTA, socials and footer land in Phase 8.
 */
export function ContactSection() {
  return (
    <section id="contact" className={styles.placeholder}>
      ContactSection
    </section>
  );
}
