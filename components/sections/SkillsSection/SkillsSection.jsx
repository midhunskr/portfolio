import styles from './SkillsSection.module.css';

/**
 * Skills section shell (Phase 1 placeholder).
 * Real filter bar and constellation land in Phase 6.
 */
export function SkillsSection() {
  return (
    <section id="stack" className={styles.placeholder}>
      SkillsSection
    </section>
  );
}
