import styles from './JourneySection.module.css';

/**
 * Journey section shell (Phase 1 placeholder).
 * Real timeline, scroll-linked fill and alternating cards land in Phase 4.
 */
export function JourneySection() {
  return (
    <section id="journey" className={styles.placeholder}>
      JourneySection
    </section>
  );
}
