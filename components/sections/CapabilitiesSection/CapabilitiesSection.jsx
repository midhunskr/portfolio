import styles from './CapabilitiesSection.module.css';

/**
 * Capabilities section shell (Phase 1 placeholder).
 * Real tab bar and crossfading panels land in Phase 6.
 */
export function CapabilitiesSection() {
  return (
    <section id="capabilities" className={styles.placeholder}>
      CapabilitiesSection
    </section>
  );
}
