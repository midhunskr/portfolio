import styles from './ProjectsSection.module.css';

/**
 * Projects section shell (Phase 1 placeholder).
 * Real accordion cards, mockup registry and showcase modal land in Phase 5.
 */
export function ProjectsSection() {
  return (
    <section id="work" className={styles.placeholder}>
      ProjectsSection
    </section>
  );
}
